#include "main.h"
/**
 * _islower - chechks for lowercase character
 * @c: the charater to check
 * Return: 1 if c is lowercase,otherwise 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
